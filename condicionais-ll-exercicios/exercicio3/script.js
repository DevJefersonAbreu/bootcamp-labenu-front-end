//códigos a serem reescritos

// a)
if(nome === "José"){
	console.log("Oi, Zé!")
} else {
	console.log("Olá, " + nome)
}


// b)
if(idade >= 18){
	console.log("Pode tirar carteira de motorista!")
} else {
	console.log("Ainda não pode tirar carteira de motorista!")
}
// Definição das variáveis para teste
const nome = "José"; // Você pode mudar o valor para testar diferentes nomes
const idade = 20; // Você pode mudar o valor para testar diferentes idades

// a) Reescrevendo o código com if ternário
console.log(nome === "José" ? "Oi, Zé!" : "Olá, " + nome);

// b) Reescrevendo o código com if ternário
console.log(idade >= 18 ? "Pode tirar carteira de motorista!" : "Ainda não pode tirar carteira de motorista!");
